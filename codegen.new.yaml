schema:
  - ${NEXT_PUBLIC_GRAPHQL_URI_NEW}

generates:
  ./src/api/graphql/schema.new.json:
    plugins:
      - introspection

  ./src/api/graphql/generated/types.new.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      typesPrefix: I
