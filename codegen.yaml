schema:
  - ${NEXT_PUBLIC_GRAPHQL_URI_LEGACY}

generates:
  ./src/shared/graphql/schema.json:
    plugins:
      - introspection

  ./src/api/graphql/generated/types.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      typesPrefix: I
